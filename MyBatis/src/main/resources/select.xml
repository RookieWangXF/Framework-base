<?xml version="1.0" encoding="UTF-8"?>
<pharos>
    <sql id="select" att="int">
        SELECT * FROM reader WHERE 1=1
        <if test="money!=null">
            and money>?
            <if test="money!=null">
                and innerMoney=?
            </if>
        </if>
        <if test="money!=null">
            and age>?
        </if>
        <if test="ad!=null">
            and height=?
        </if>
    </sql>
    <sql id="delete">
        SELECT * FROM reader WHERE 2=2
        <if test="money!=null">
            and money>?
        </if>
    </sql>
    <sql id="where">
        SELECT * FROM reader
        <where>
            <if test="">
                and money>?
            </if>
            <if test="aa">
                and mo>?
            </if>
        </where>
        and leilei>243
    </sql>
    <sql id="set">
        UPDATE reader
        <set>
            <if test="">
                money=?,
            </if>
            <if test="readerID!=null">
                userID=userID,
            </if>
        </set>
        <where>
            <if test="213">
                and money>?
            </if>
            <if test="aa">
                and mo>?
            </if>
        </where>
    </sql>
    <update id="selectbyid">
        SELECT * FROM reader WHERE 2=2
        <if test="money!=null">
            and money>?
        </if>
    </update>
</pharos>